# Hello World (Classroom 50 Autograder Demo)

## Goal
Create a file named `hello.py` that contains a function `hello()` which returns the string `"Hello, world!"`.

## Starter code (copy this)

Create a new file called `hello.py` and paste the following into it:

```python
def hello():
    return "Hello!"   # ← change this so the test passes